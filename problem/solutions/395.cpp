#include<bits/stdc++.h>
using namespace std;
char a[305];
int main(){
    for(int i=0;i<305;i++){
        char b[100]={0};
        cin>>b;
        int len=strlen(b);
        if(len==0)break;
        if(i==0)cout<<len;
        else cout<<","<<len;
        len=0;
        
    }
	return 0;
}